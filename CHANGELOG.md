# Changelog

All notable changes to **Glitter List** are documented here. The format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and this project aims to adhere to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

## [0.2.0] - 2026-04-29

### Changed

- **Glitter photo backgrounds.** Replaced the flat pink (`#FFD8F3`) and dark purple (`#2A1A3E`) page backgrounds with two real glitter textures — bright pink-magenta in light mode, deep iridescent purple in dark mode. The image fills the entire window full-bleed: status bar, AppBar, and list body all sit on the same texture for a single continuous surface. The original solid colors stay in `ColorScheme.surface` so dialogs, popup menus, and the modal bottom sheet keep their opaque chrome.
- **Diffusion scrim** softens the texture behind text. A 30%-alpha surface-tinted layer is composited over the image via `DecorationImage.colorFilter`, mixing the saturated glitter back toward the brand color so todo titles stay legible without losing the sparkle.
- **Background slides with the swipe.** The background is wired to the `PageController` via `AnimatedBuilder`; as the user swipes between lists, the image's horizontal alignment pans in lockstep with the page offset (`Alignment(-1, 0)` at the first list, `Alignment(1, 0)` at the last). With `BoxFit.cover` already cropping horizontal slack, the effect feels like the glitter is one continuous surface that the lists glide across.
- **Background slides vertically too.** A `NotificationListener<ScrollNotification>` wraps the `PageView` and tracks the active list's `ScrollMetrics`; the vertical pan is fed into a `ValueNotifier` merged into the same `Listenable` that drives the `AnimatedBuilder`, so the bg pans down as the user scrolls through a long list. The mapping is `alignmentY = -1 + 2 * pixels / max(maxScrollExtent, 2 * viewport)`, which keeps the bg at ~15% of text-scroll speed for short lists (matching the slow parallax feel of the horizontal swipe — bg slow, text fast) and even slower per pixel for very long lists. `_verticalT` initializes to `-1` and resets to `-1` on page change, so an unscrolled or just-swiped-in list always starts with the top of the texture visible. The bg image is rendered through a `Transform.scale(1.3)` inside a `ClipRect` so there's pan slack on both axes — plain `BoxFit.cover` on a portrait image gives no vertical slack to pan into.
- **Custom left-edge scroll indicator.** When a list is long enough to scroll, a thin 3-px-wide bar in the content text color appears 9 px from the screen's left edge — half-way between the margin and the checkbox column. The thumb's height tracks `viewport / total content`, its vertical position tracks `pixels / maxScrollExtent`, and it hides automatically on lists that fit on one screen. `ListPage` now owns its own `ScrollController` so the indicator (and any future scroll-tracking chrome) has a stable handle.
- **Per-glyph cloud diffusion replaces the global scrim.** The full-screen surface-tint scrim is gone — bg stays raw past ~15 px from any letter. Each glyph carries a stack of four Gaussian shadows of the surface color (sigmas `[12, 5, 2, 2]` px), centralized in the new `GlitterShadows.aroundText(surface)` helper. A single shadow caps at ~50% local alpha at the glyph edge (the integral of a Gaussian over the filled glyph silhouette), so stacking is what builds the cloud up to a near-opaque core. Cumulative srcOver alpha at distance `d` (px) from the glyph: ~94% at 0, ~85% at 1, ~58% at 3, ~48% at 4, ~18% at 10, ~0% past 20. Applied to `HomePage`'s AppBar title and `TodoTile`'s body text (the inline edit `TextField` and the static `RainbowStrikethrough` both consume the same `baseStyle`).
- **Light-mode content text flipped back to dark.** The earlier unification put pale lilac (`#D4BBEF`) on both modes, but it washes out on the bright pink glitter. Light mode now uses `#2A1A3E` (matches `darkBg`) for content text — near-black silhouette against the pink — while dark mode keeps the pale lilac. Content joins bg image and AppBar / page-dot chrome as the per-brightness selectors; the FAB accent is still constant.
- **Asymmetric parallax.** Replaced the uniform `Transform.scale(1.3)` on the bg layer with `Transform(Matrix4.diagonal3Values(1.48, 1.39, 1))`: ~60% more horizontal slack than the previous tuning and ~30% more vertical slack, so horizontal swipes get a bigger bg swing than vertical scrolls — proportional to how much further the eye travels per swipe vs. per scroll tick.
- **Saturation pop on the bg image.** A `ColorFilter.matrix` (s = 1.3, Rec. 709 luminance weights) sits in `DecorationImage.colorFilter` ahead of the scrim layer. Closest practical "HDR-style" punch we can do today — Flutter's wide-gamut / HDR image pipeline is still platform-limited (iOS-leaning, Android patchy), so this is a synthetic boost in sRGB rather than true HDR rendering.
- **Unified content color.** Light mode no longer uses the dark-magenta (`#A32D6E`) content color — both modes now share the pale lilac (`#D4BBEF`) text that dark mode already had. The pink-magenta on the new bright glitter was uncomfortably low-contrast; the lilac reads cleanly on both backgrounds. Only the bg image and the AppBar / page-dot chrome still vary with brightness.
- **Per-line frosted blur behind text.** New reusable widget `PerLineBackdropBlur` (`lib/ui/per_line_backdrop_blur.dart`) lays out a `TextPainter` once at the available constraint, pulls per-line metrics, and emits a `Stack` of `Positioned` `ClipRRect → BackdropFilter → Text` strips — one per line, each tight to that line's actual content width. Wrapped multi-line items now have N tight strips instead of one rectangle. Wired into the AppBar title (`HomePage`, ungrouped — AppBar isn't scrollable) and into `RainbowStrikethrough` (`lib/ui/check_animation.dart`, grouped — sits inside the scrollable). The strikethrough painter's own `TextPainter` continues to align rainbow lines correctly because `PerLineBackdropBlur` sizes itself to the same `tp.size` that a plain `Text` would have produced.
- **`BackdropGroup` + `BackdropFilter.grouped`.** Wrap the body's `PageView` in a `BackdropGroup`; per-tile strips use `BackdropFilter.grouped` so the N filters in the visible page share one backdrop snapshot per frame instead of forcing N independent rasterizations. Eliminates the vertical-scroll re-rasterization tearing that the ungrouped pattern hit (Flutter `#104769`, `#138615`, `#141510`, `#161262`). Available since Flutter 3.35; we're on 3.41 so no SDK floor change.
- **`TileMode.decal` on every blur.** Defense in depth against the edge-bleed flicker tracked in Flutter `#160648`. Cheap, removes a known artifact at the rounded corners of each frosted strip.
- **Seed sample data on first launch.** Five themed lists with ~45 items total — Self-Care ✨, Wishlist 💖, Groceries 🛒, Weekend 🌷, Tidy Up 🧹 — replace the previous single empty "My Glitter List ✨". Skipped on subsequent launches once the Hive box has anything.
- **Pre-baked frosted-bg replaces live `BackdropFilter`.** New `lib/ui/baked_bg.dart` async-bakes a single `ui.Image` at viewport size with the saturation matrix and Gaussian blur applied (Riverpod `FutureProvider.family` keyed by brightness + size, auto-disposes the image when watchers detach). New `lib/ui/pre_baked_backdrop.dart` is a leaf `RenderBox` that paints a slice of that pre-baked image at its current screen position via `canvas.drawImageRect`. `PerLineBackdropBlur`'s per-line strips wrap one each. There is no live `BackdropFilter` left in the scrolling tree, so the engine re-rasterization race that produced the vertical-scroll tearing (Flutter `#138615` / `#141510`) can't fire. Live bg parallax (horizontal + vertical) updates on every frame again — no need to freeze it during scroll. `BackdropGroup` is gone too. Inline-edit `TextField` drops its blur for now (per-line splitting doesn't work mid-typing; not worth the complexity for the brief edit window).



First versioned cut of the app. Local-only multi-list todos with a glittery, offline-first Flutter build for Android and iOS.

### Added

- **Multi-list todos.** Create named lists, swipe between them with a page indicator, reorder items via a drag handle, toggle completion, edit item text in place.
- **Offline-first storage** with Hive (community fork `hive_ce`): every change persists to disk immediately; lists survive app restarts, no network calls anywhere.
- **Custom pink / purple color palette** with automatic light/dark mode support. Light mode: pink scaffold with dark-pink content text; dark mode: deep-purple scaffold with light-purple content text. Hot-pink accent for FAB and checkboxes.
- **Sniglet** as the app typeface, bundled as TTF assets so the app has zero runtime font fetch. Ships fully usable offline on first launch.
- **Check-off animation.** Tapping an item plays four parallel effects over 1 second: a rainbow strikethrough draws across the text, the checkbox glows with a bell-curve burst, ten sparkles emanate outward from the checkbox, and the item text crossfades to its muted "done" color. Multi-line items draw the strikethrough sequentially per line, with a single rainbow gradient that continues seamlessly across all wrapped lines.
- **Hamburger menu** with enlarged labels and icons: **New List**, **Rename List**, **Clear Completed** (visible only when there's something to clear, with a count-based confirm dialog), **Delete List**.
- **Long list titles wrap.** The AppBar grows vertically to fit titles that exceed one line (up to three).
- **Inline edit.** Tap any item to edit its text; the cursor appears in place with no font size or color shift.
- **Developer error handlers** that capture framework and async exceptions with a `[glitter-error]` log prefix for easy grep.
- **Custom app icon** replacing the default Flutter launcher icon. Single 1254×1254 opaque composite (sparkly pink-purple gradient, notepad + pen + gem) used as the master for both platforms. Android wraps it in a minimal adaptive-icon XML using the composite as the background layer with a transparent foreground — the launcher masks the whole thing to the device's configured shape (circle, squircle, rounded square) without the Android 8+ "legacy bitmap in a white circle" fallback. iOS applies its own rounded-rect mask.

### Fixed

- Dialog `TextEditingController` lifecycle race that tripped Flutter's internal `_dependents.isEmpty` framework assertion when tapping **Cancel** during the dialog's exit animation.
- Default Android FAB contrast on the pink scaffold (it was invisible against the auto-generated pale-pink `primaryContainer`).

### Notes

- iOS builds require macOS + Xcode; they are not producible on the Linux development host. The `ios/` scaffold is kept up to date so the code is iOS-ready when a Mac or a CI macOS runner is wired in.
- Android release signing still uses the `flutter create` debug keystore. A proper release signing config will be set up before the first Play Store submission.
